const termEnum = {
  first: 1,
  second: 2,
  third: 3,
};

const ADMISSION_STATUS = {
  PENDING: "pending",
  ADMITTED: "admitted",
  REJECTED: "rejected",
};

export { termEnum, ADMISSION_STATUS };
